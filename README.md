# Model-Rocket-Apoapsis
Calculates the apoapsis and the time to apoapsis of a model rocket.

With Terminal or command line enter the command 

cd Model-Rocket-Apoapsis
make
./run (terminal) 
start run (command line)
